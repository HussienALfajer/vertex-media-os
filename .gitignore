# Dependencies
node_modules/

# Build and tool output
dist/
out-tsc/
*.tsbuildinfo
test-output/
coverage/
.vitest/
.vite/
playwright-report/
apps/api/generated/
# Vite's temporary bundles of its config files; TanStack Router plugin scratch files
vite.config.*.timestamp*
vitest.config.*.timestamp*
.tanstack/

# Generated Prisma client (regenerate with `pnpm db:generate`)
packages/database/src/generated/

# Nx (caches and workspace data only; nothing under .nx is source)
.nx/

# Local environment (secrets never committed; `.env.example` is tracked)
.env
.env.*
!.env.example

# Editors and OS
.idea/
.vscode/*
!.vscode/extensions.json
.DS_Store
Thumbs.db
*.log
*.tmp
tmp/
